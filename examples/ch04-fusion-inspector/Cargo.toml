[package]
name = "ch04-fusion-inspector"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
burn = { workspace = true, features = ["cpu", "fusion"] }
burn-fusion = { workspace = true, features = ["std", "test-util"] }

