# 第 4 章来源映射

快照：OpenMLSys `9c289782ccbb165ac8ad7c960ecffc12942a5560`；
Burn `976aa9c5ec1d2dd3412710f99759e3c44bdff03d`；
CubeCL `be278a1e76aed881e2cc6b165414ee6103ca4634`。

## OpenMLSys 来源

下列 OpenMLSys 路径均相对于 `openmlsys/v1/zh_chapters/`。

### `chapter_frontend_and_ir/`

| 来源文件 | 保留的思想 | 主要改写 |
|---|---|---|
| `ai_compiler_design_principle.md` | AOT/JIT、Pass、前后端分层、多层 IR | 用 Burn IR/Fusion/CubeCL 栈替换 TensorFlow、MindSpore 与 Ascend 主线 |
| `overview_of_frontend.md` | 前端负责语义、IR、静态信息与优化 | 压缩解析器内容，聚焦张量程序到可执行计划 |
| `intermediate_representation.md` | 线性/图/混合 IR、SSA、机器学习 IR 的需求 | 删除框架 IR dump；区分 autodiff tape、OperationIr、CubeCL Scope |
| `ad.md` | 自动微分与可变换表示的关系 | 求导原理已在第 2 章；本章只讨论 tape 与编译 IR 边界 |
| `type_system_and_static_analysis.md` | 类型、抽象解释、常量与 shape 信息支持优化 | 用 Rust 编译期类型和运行时 TensorIr 元数据替换 MindIR 静态分析 |
| `common_frontend_optimization_pass.md` | DCE、常量传播/折叠、CSE 与 Pass 管线 | 用中性 IR 例子；不宣称 Burn Fusion 实现全部经典 Pass |
| `index.md`、`summary.md` | 学习目标和前端职责总结 | 合并到本章导言与小结 |

### `chapter_backend_and_runtime/`

| 来源文件 | 保留的思想 | 主要改写 |
|---|---|---|
| `overview.md` | 图优化、Kernel 选择、内存、调度执行流水线 | 映射到 burn-fusion、burn-cubecl-fusion 与 CubeCL Runtime |
| `graph_optimizer.md` | 融合减少中间结果和访存；等价子图替换 | 用 ElementWise/Matmul/Reduce fuser 替换 MindSpore Graph Kernel |
| `kernel_selecter.md` | dtype、layout、设备能力与候选 Kernel | 衔接 CubeK Strategy 与 CubeCL 编译键 |
| `memory_allocator.md` | 生命周期、内存池、复用与 in-place 条件 | 用 TensorStatus、HandleContainer 与 CubeCL memory pools；通信内存后移第 6 章 |
| `compute_schedule_and_execute.md` | eager/graph、拓扑依赖、异步、stream、副作用 | 用 Flex eager 与 Fusion stream；不映射 Ascend task 下沉 |
| `op_compiler.md` | compute/schedule、tiling、搜索与代码生成 | 连接 CubeCL Scope、optimizer、Compiler 和 JIT；TVM 仅作生态对照 |
| `index.md`、`summary.md` | 后端范围与扩展阅读 | 合并到导言、边界和来源说明 |

OpenMLSys v2 第 4 章固定快照只有“IR 设计、图优化、算子生成与运行时执行”
TODO，没有可迁移正文。本章不复制 v1 的 MindSpore/Python/TVM 代码或
ch04/ch05 图片，结构图使用原创文本图。

## Burn IR 与 Fusion 事实来源

| 结论 | 固定源码 |
|---|---|
| OperationIr、TensorIr、TensorId 与 TensorStatus 描述延迟操作和资源状态 | `burn/crates/burn-ir/src/operation.rs`、`tensor.rs` |
| Fusion Tensor op 注册 OperationIr，而不是立即执行底层算子 | `burn/crates/burn-fusion/src/ops/` |
| GlobalFusionClient 将操作注册到按 stream 管理的 FusionServer | `burn/crates/burn-fusion/src/client.rs`、`server.rs` |
| MultiStream/OperationQueue 搜索并执行优化块或 unfused 操作 | `burn/crates/burn-fusion/src/stream/`、`search/` |
| CubeCL Backend 注册 ElementWise、Matmul、Reduce 等 fuser | `burn/crates/burn-cubecl/src/fusion.rs`、`burn-cubecl-fusion/src/optim/` |
| 读回和 `Device::sync` 会 drain 对应 stream，形成物化边界 | `burn/crates/burn-fusion/src/server.rs`、`client.rs` |
| Flex 直接执行，不产生 Fusion OperationIr | `burn/crates/burn-flex/ARCHITECTURE.md`、`burn/crates/burn-flex/src/` |
| FusionInspector 是 `test-util` 下的测试观测 API，不是生产接口 | `burn/crates/burn-fusion/src/inspect.rs` |
| add→exp 融合与同步切分的上游回归模式 | `burn/crates/burn-backend-tests/tests/fusion/fusion_shape.rs` |

## CubeCL 编译与 Runtime 事实来源

| 结论 | 固定源码 |
|---|---|
| `#[cube]` 宏生成 expand，向 `cubecl_ir::Scope` 注册指令 | `cubecl/crates/cubecl-macros/src/generate/` |
| KernelBuilder 将 Scope 与 IO/设置集成为 KernelDefinition | `cubecl/crates/cubecl-core/src/compute/`、`cubecl/crates/cubecl-runtime/src/kernel.rs` |
| 不同 Compiler 使用不同优化/Lowering 管线 | `cubecl/crates/cubecl-spirv/src/compiler.rs`、`cubecl/crates/cubecl-cpp/src/`、`cubecl/crates/cubecl-cpu/src/` |
| CubeCL 当前以 JIT 和编译缓存为主，没有统一一等 AOT API | `cubecl/README.md`、`cubecl/crates/cubecl-runtime/src/compiler.rs` |
| ComputeClient launch 入队，read/sync/flush 构成等待边界 | `cubecl/crates/cubecl-runtime/src/client.rs` |
| Runtime 含 sliced/exclusive/persistent 等内存池 | `cubecl/crates/cubecl-runtime/src/memory_management/` |

## 2026-08-01 补全

- `ch04/02`：常量传播 → DCE 顺序手推（中性伪 IR）。
- `ch04/06`：融合 vs sync 切分的生命周期条带图；强调 ReadWrite 非充分条件。
- 示例：`inspect_add_mul_exp` 断言三操作 ElementWise block。
- 编译栈地图与术语表完成边界用语对齐。
- `ch04/02`：补充 Pass 的输入/分析/输出/回退契约，明确经典 Pass 名称
  不等于固定 Burn Fusion 已实现。
- `ch04/05`：补充 op/shape → strategy/tune key → KernelDefinition/
  compile cache → launch/read 的因果链，区分各类缓存与完成边界。
- 未迁入：SOMAS / Graph Kernel / 长 TVM 教程。

## 本章不作出的承诺

- 不把 autodiff tape、Burn Fusion IR、CubeCL Scope 和设备 graph capture
  称作同一张图。
- 不声称 Burn Fusion 实现全部传统编译器 Pass。
- 不把 FusionInspector 的 block 数称为硬件 Kernel launch 计数。
- 不声称 Flex 路径具有 Fusion IR。
- 不声称所有 CubeCL Compiler 共用同一优化管线或最终 IR。
- 不声称 CubeCL 提供统一的完整 AOT 工具链。
- 不以一次 CPU 融合实验的时间差作为普遍性能结论。

## Crosswalk 与证据状态

逐文件对照见 [`../comparison/openmlsys-v1-crosswalk.md`](../comparison/openmlsys-v1-crosswalk.md)
的“前端与 IR”和“后端与运行时”段。FusionInspector 的计划结构和
数值等价属于 `源码核验 + CPU 可运行验证`；OperationIr/cache/lifetime
追踪属于 P1 观察，compile backend、AOT 和真实硬件 launch 仍是
`源码核验/可选平台实验`。

对应比较卡位于 `book/src/comparison-cards.md` 的“IR、Fusion、cache 与
launch”；重复计划测试只比较结构和输出，不断言 cache hit 或第二次更快。

